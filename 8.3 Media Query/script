*For Linux Users:*
github-upload () {
    /usr/bin/git add .
    if [ $# -eq 0 ]; then
        /usr/bin/git commit -m "updated"
    else
        shift
        /usr/bin/git commit -m "$@"
    fi
    /usr/bin/git push
}

1- add it to your ~/.bashrc file and save it
2- use the command *" source ~/.bashrc "*

*For Windows Users: (Using Git Bash)*
github-upload () {
    git add $PWD
    if [ $# -eq 0 ]; then
        git commit -m "updated"
    else
        shift
        git commit -m "$@"
    fi
    git push
}

1- add it to your ~/.bashrc or ~/.bash_profile file and save it
2- use the command *" source ~/.bashrc "*

USAGE: github-upload <commit message> (no need for quotes)